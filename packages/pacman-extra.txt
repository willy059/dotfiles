# Paquets installés explicitement à la main après l'installation de base
# CachyOS (déduit de /var/log/pacman.log, transaction de calamares
# terminée à 14:42:39) — hors dépendances, pour ne pas risquer qu'une
# dépendance soit un jour marquée "explicite" par erreur. Déjà inclus
# dans pacman.txt (qui capture l'état complet) — ce fichier sert juste de
# repère lisible sur ce que tu as ajouté toi-même.
brave-bin                           # navigateur
extension-manager                   # gérer les extensions GNOME
gnome-shell-extension-dash-to-dock  # dock façon macOS (pas encore activée, voir README)
claude-code                         # CLI Claude Code
papirus-icon-theme                  # thème d'icônes
spotify-launcher                    # client Spotify
octopi                               # frontend graphique pour pacman
mpv                                  # lecteur vidéo (remplace showtime)
