# Paquets installés à la main après l'installation de base CachyOS
# (déduit de /var/log/pacman.log : tout ce qui a été installé après la
# transaction de calamares terminée à 14:42:39). Déjà inclus dans
# pacman.txt (qui capture l'état complet) — ce fichier sert juste de
# repère lisible sur ce que tu as ajouté toi-même.
libxss                              # dépendance de brave-bin
brave-bin                           # navigateur
extension-manager                   # gérer les extensions GNOME
gnome-shell-extension-dash-to-dock  # dock façon macOS (pas encore activée, voir README)
claude-code                         # CLI Claude Code
